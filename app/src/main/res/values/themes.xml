<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GitHubTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@color/colorPrimary
        </item> <!-- Set your status bar color here -->
    </style>

    <style name="ButtonSplash" parent="Base.Theme.GitHubTask">
        <item name="android:paddingVertical" tools:targetApi="o">15dp</item>
        <item name="textFillColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@drawable/button_corner</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.GitHubTask" parent="Base.Theme.GitHubTask" />
</resources>